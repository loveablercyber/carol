# Notifications (email + WhatsApp)
RESEND_API_KEY=
NOTIFICATION_EMAIL_FROM=
WHATSAPP_CLOUD_ACCESS_TOKEN=
WHATSAPP_CLOUD_PHONE_NUMBER_ID=

# Optional: secure appointment reminder cron endpoint on Vercel
CRON_SECRET=

# Hours before scheduled time required to keep appointment confirmed.
# If not confirmed by client within this window, appointment is auto-cancelled and slot is released.
APPOINTMENT_CONFIRMATION_DEADLINE_HOURS=12
